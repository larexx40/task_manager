type User {
  _id: ID!
  name: String!
  email: String!
  username: String!
  age: Int
  phoneNumber: String
}

extend type Query {
  getUser(id: ID!): User!
}

extend type Mutation {
  createUser(name: String!, email: String!): User!
  # Add other user mutations here
}